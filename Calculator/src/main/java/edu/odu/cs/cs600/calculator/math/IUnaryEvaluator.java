package edu.odu.cs.cs600.calculator.math;

public interface IUnaryEvaluator {
	// TODO: Remove this method, and instead just use the interface as a marker rather than a contract
	public double compute(double value) throws ArithmeticException;
}
